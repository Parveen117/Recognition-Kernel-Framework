# Recognition Kernel Completion of the Five-Label Weil Endpoint

This folder is the updated current paper surface built from the transferred MP theorem capsules in `theorum/` and the reusable proof machinery in `mp_gold/`.

## Main editorial decision

The paper no longer follows the vulnerable route through unshifted even-source positivity. It is rewritten around the corrected theorem chain:

```text
source-restriction / blind-kernel minimality
-> positive-eta amplitude path equality
-> seam integer and common five-matrix reduction
-> exact M3 positive accepted block
-> one-sector terminal reduction
-> zero-cut T03 endpoint completion
-> classical completed-Weil / Weil criterion membrane
```

## Files

```text
main.tex        updated paper draft
references.bib  minimal bibliography shell
```

## Claim discipline

The draft foregrounds RKF as a minimal anti-blindness architecture rather than a decorative framework. Each layer removes one typed obstruction:

```text
source restriction;
blind kernel;
normalization/path ambiguity;
finite accepted-block uncertainty;
endpoint support/source-readout defect.
```

The paper preserves the boundary that the classical completed explicit formula and Weil criterion are the terminal classical membrane. The native contribution is the completed-Weil endpoint route through the transferred theorem capsules.
