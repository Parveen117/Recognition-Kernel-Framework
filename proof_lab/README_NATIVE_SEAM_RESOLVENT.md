# Native Seam Resolvent (theorum/52)

Resolvent and spectral region on the primitive carrier, from T01 mass
submultiplicativity + theorum/28 recognition-Cauchy with declared Smriti
tails + exact K_Sigma arithmetic. Seam gap becomes a native spectral region
G_rho = {N(lambda) <= rho f0 D(lambda)} with f0 outside; product locus
uniform in m via theorum/50's sheet-mass law.

```bash
python proof_lab/native_seam_resolvent.py
python -m unittest proof_lab.test_native_seam_resolvent -v
```
Pin in `NATIVE_SEAM_RESOLVENT_EXPECTED.sha256`.
