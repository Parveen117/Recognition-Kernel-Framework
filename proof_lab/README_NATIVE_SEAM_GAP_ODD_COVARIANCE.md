# Native Seam Gap + Odd-Sector Covariance (theorum/50)

Primitive carrier only: C_Sigma scalars (rad, turn), path convolution,
native dagger, verdicts by cut-tail mass M_Sigma and recognition energy
E_Sigma (RH-Framework T01).  Faces are generated by the flow (RST-1 Cayley
step).  Part I: the product's memory sheets have EXACTLY multiplicative
mass <= rho^k f0^m and contract in energy by rho^2 f0^(2m), uniformly in
m.  Part II: the cut square L^dagger * L has a certified odd channel --
turn-free diagonal, reversal-antisymmetric, orientation-covariant, Leibniz
across faces, negative feedback into the even channel -- invisible to every
quadratic form, mass and energy.  A test greps the source for any Hilbert
verdict.

```bash
python proof_lab/native_seam_gap_odd_covariance.py
python -m unittest proof_lab.test_native_seam_gap_odd_covariance -v
```
Expected status `PASS_NATIVE_SEAM_GAP_ODD_COVARIANCE_CANDIDATE`; pin in
`NATIVE_SEAM_GAP_ODD_COVARIANCE_EXPECTED.sha256`.
