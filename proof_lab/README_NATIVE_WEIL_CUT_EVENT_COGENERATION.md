# Native Weil Cut-Event Co-Generation Stage 3A

This is the first RH-specific stage after the generalized Recognition-Seam,
finite-to-infinite, minimal-observer and cut-covariance stages passed.

It does **not** claim that the actual completed-Weil boundary has already been
oriented on the prime--Gamma event carrier.  It proves the exact theorem that
such an orientation must satisfy, audits the direct unshifted scalar reserve,
and fails closed until the missing coefficientwise map is supplied.

## Native objects pinned from the source manuscript

```text
odd carrier                 X3^-
boundary functional         L_partial f = sqrt(2) f-hat(i/2)
odd source                  S_(0,-)^full
source analysis statement   Xi_0* Xi_0 = S_(0,-)^full
completed odd form          W_3^- = S_(0,-)^full - L_partial* L_partial
```

## New theorem packet

```text
theorum/33_native_weil_cut_event_cogeneration_gate.md
proof_lab/native_weil_cut_event_cogeneration.py
proof_lab/test_native_weil_cut_event_cogeneration.py
proof_lab/NATIVE_WEIL_CUT_EVENT_COGENERATION_EXPECTED.sha256
proof_lab/imported/T21_DIRECT_UNSHIFTED_CUT_BURDEN_ACTUAL.json
```

The exact calibrations verify:

```text
1. source and boundary generated by one finite event lift;
2. constructive unit boundary-state enlargement;
3. cut covariance and boundary-line no-blindness;
4. exact shifted-to-unshifted event identity;
5. coherent refinement equality;
6. same-mass opposite-orientation negative control;
7. nonproportional refinement burden increase;
8. T21 direct-unshifted Decimal arithmetic;
9. robust orientation-residual budget;
10. fail-closed RH specialization contract.
```

## Pull and run in Jupyter

```python
%cd "<LOCAL_PATH>/Recognition-Kernel-Framework"

!git fetch origin
!git switch main
!git pull --ff-only origin main

!python -m unittest -v proof_lab.test_native_weil_cut_event_cogeneration

!python -m proof_lab.native_weil_cut_event_cogeneration \
    --output proof_lab/NATIVE_WEIL_CUT_EVENT_COGENERATION_ACTUAL.json
```

Expected:

```text
Ran 7 tests
OK
PASS_NATIVE_WEIL_CUT_EVENT_COGENERATION_STAGE3A
```

## Canonical hash

```python
from hashlib import sha256
from pathlib import Path

expected = Path(
    "proof_lab/NATIVE_WEIL_CUT_EVENT_COGENERATION_EXPECTED.sha256"
).read_text(encoding="ascii").strip()

actual_bytes = Path(
    "proof_lab/NATIVE_WEIL_CUT_EVENT_COGENERATION_ACTUAL.json"
).read_bytes()

actual = sha256(actual_bytes).hexdigest()
print("HASH_STABLE", expected == actual)
print("EXPECTED", expected)
print("ACTUAL  ", actual)
```

Expected:

```text
HASH_STABLE True
```

## Direct-unshifted reserve

The imported T21 scalar packet records

```text
beta-zero upper        0.8290856201657449
reserve lower          0.1709143798342551
active cells           2400
prime-power events     78734
```

The robust orientation theorem gives the still-available source-norm residual
budget

```text
1 - sqrt(beta upper)
= 0.0894586115031646185...
```

Thus the next actual task is not to improve the scalar beta. It is to construct
or outwardly enclose the same-event orientation residual

```text
r_N(f) = L_partial f - <c_partial,N, Xi_0 f>
```

and prove

```text
|r_N(f)| <= delta_N ||Xi_0 f||
```

with

```text
delta_N < 0.0894586115031646...
```

or prove the exact identity with `delta_N = 0`.

## Claim boundary

A pass proves the universal finite event construction, refinement theorems,
robust orientation inequality and the arithmetic audit of the transferred T21
reserve. It does not yet prove the actual prime/Gamma/debt event orientation,
the actual completed-Weil cut covariance, completed-Weil no-blindness, or RH.
