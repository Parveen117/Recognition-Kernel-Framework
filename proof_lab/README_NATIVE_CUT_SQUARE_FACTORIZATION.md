# Native Cut-Square Factorization (theorum/53)

S = L^dagger*L = C^dagger Dg C exactly over K_Sigma: positivity in the
turn-free weights, the odd sector in the factor phases; native Parseval;
exact negative witness for non-squares; seam-face weights bounded by
(rho f0)^2 weight by weight; factorization tensors over products; prod of
weights = det S invariant under the unitary flow.

```bash
python proof_lab/native_cut_square_factorization.py
python -m unittest proof_lab.test_native_cut_square_factorization -v
```
Pin in `NATIVE_CUT_SQUARE_FACTORIZATION_EXPECTED.sha256`.
