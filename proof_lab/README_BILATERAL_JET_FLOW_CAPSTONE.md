# Bilateral Jet-Flow Capstone Certificate

This packet connects the two strongest abstract developments already present on
`agent/cut-graded-lambda-jacobian-tower`:

1. `theorum/41_cut_graded_universal_generator_theorem.md`;
2. `theorum/42_cut_graded_lambda_jacobian_tower_theorem.md`.

It does not introduce a physical nuclear law. It supplies the theorem-level
interface that a later nuclear adapter must satisfy.

## Certified finite shadow

The exact rational fixture uses

```text
J = diag(1,-1,1,-1)
G = four-step nilpotent shift
B = first-coordinate observer
```

so that

```text
J G J = -G,
G^4 = 0,
B G^k J = (-1)^k B G^k.
```

The packet verifies:

- exact bilateral flow `J exp(tG) J = exp(-tG)`;
- the exponential cut-square identity;
- forward/backward observer strands and zero closure defect;
- alternating cut parity of all four finite jet layers;
- exact third-order reconstruction of the observed nilpotent flow;
- a nonzero second-order remainder negative control;
- strict observer-rank growth `1,2,3,4`;
- kernel-dimension reduction `3,2,1,0`;
- repair of a target blind through second order;
- minimum decoder burden `1/4` and reserve `3/4`;
- observer consumption of the cut-loop seam-curvature coefficient;
- fail-closed capstone classifications.

## Run

From the repository root:

```bash
python -m unittest -v proof_lab.test_bilateral_jet_flow_capstone
python -m proof_lab.bilateral_jet_flow_capstone \
  --output proof_lab/BILATERAL_JET_FLOW_CAPSTONE_ACTUAL.json
```

Expected status:

```text
PASS_BILATERAL_JET_FLOW_CAPSTONE_CANDIDATE
```

Expected SHA-256:

```text
4f0f31183c6de9b354fbc90134cfcd14c8d6f55270f41c1e286f719c0e06b091
```

## Claim boundary

This exact certificate does not prove:

- an unbounded generator domain theorem;
- identification of the nuclear cut;
- that the two bilateral strands are already a physical double helix;
- a Madhava-named calculus absent from the current repository tree;
- that higher jet order improves every physical prediction.

The later physical adapter must declare the state space, response bundle, cut,
observer, target, ordering variable, remainder topology and abstention rule.
