# RH Target Gate: Memory Cut Rank and Source-Defect Identification

## Purpose

The universal Cut-Memory Spectral Isomorphism theorem is complete once the
completed-Weil operator has the form

\[
W_3^-=R-V_\Sigma V_\Sigma^*
\]

with \(R>0\) and \(\operatorname{rank}V_\Sigma\le5\).

The remaining RH-specific theorem is to derive this decomposition from the cut,
not to assume it from an already assembled five-column packet.

---

## Native objects to construct

### 1. Canonical cut bridge

Construct

\[
C_{\rm Weil}
=
Q\pi(a_{\rm Weil})V_0
\]

from the prime, Gamma, boundary and compatibility events before whitening or
post-hoc Gram fitting.

### 2. Source reference

Construct a positive source reference \(R\) from the cut-even second moment:

\[
R(f,h)
=
\langle P\pi(a_h)\Omega,P\pi(a_f)\Omega\rangle.
\]

The positivity of \(R\) must be independent of the desired completed-Weil sign.

### 3. Cut-memory columns

Choose an isometry

\[
J_\Sigma:\mathbb C^{\nu_\Sigma}
\longrightarrow
\overline{\operatorname{Ran}C_{\rm Weil}},
\]

and define

\[
V_\Sigma
=
\text{the source-side pullback of }J_\Sigma.
\]

The construction must prove

\[
\nu_\Sigma\le5
\]

from the source-restriction/blind-kernel theorem and the minimal five-label
repair, not from numerical rank thresholding.

### 4. Exact defect identity

Prove on the native generator core

\[
W_3^-(f,h)
=
R(f,h)
-
\langle V_\Sigma^*f,V_\Sigma^*h\rangle_{\mathbb C^{\nu_\Sigma}}.
\]

Then extend by recognition-Cauchy completion.

---

## Required theorem statements

### Theorem A (Target-relevant memory rank)

\[
\dim
\overline{\operatorname{Ran}
\left(
Q\pi(\mathcal A_{\rm Weil})V_0
\right)}
\le5.
\]

### Theorem B (Source-defect factorization)

\[
W_3^-=R-V_\Sigma V_\Sigma^*.
\]

### Theorem C (No hidden complement)

Every target-relevant negative direction of \(W_3^-\) lies in the cyclic cut
memory generated by \(\operatorname{Ran}V_\Sigma\).

Equivalently, the reference-normalized defect has no adverse component on the
orthogonal cut-memory complement.

---

## Terminal consequence

Once A--C are proved, set

\[
B_\Sigma=V_\Sigma^*R^{-1}V_\Sigma.
\]

Then the universal theorem gives

\[
W_3^-\ge0
\iff
B_\Sigma\le I,
\]

\[
N_-(W_3^-)=N_+(B_\Sigma-I),
\]

and

\[
\inf_{f\ne0}
\frac{\langle f,W_3^-f\rangle}{\langle f,Rf\rangle}
=
\lambda_{\min}(I-B_\Sigma).
\]

Thus the infinite-dimensional completed-Weil odd sign and reference-normalized
gap are decided exactly by a matrix of size at most five.

---

## Numerical development plan

The next executable packet must not merely assemble an arbitrary rank-five
matrix. It must verify the actual native identification.

```text
1. build the cut bridge from declared event amplitudes;
2. compute its singular spectrum under refinement;
3. prove five-dimensional target-relevant rank by exact source restriction;
4. construct R and V_Sigma from the same cut packet;
5. verify the form identity W3-=R-VV* on an independent test bank;
6. enclose the off-bank/operator tail;
7. compute B=V*R^-1V with outward intervals;
8. certify lambda_min(I-B)>0 or report the precise failing mode.
```

A passing finite-grid test is diagnostic until Steps 3, 5 and 6 are proved
uniformly.

---

## Negative controls

The implementation must reject:

```text
positive determinant with two super-threshold eigenvalues;
a fitted rank-five factor unrelated to the cut bridge;
a numerically rank-five packet without a source-restriction theorem;
a central projection that kills the off-diagonal bridge;
an ordinary spectral-gap equality without reference normalization;
a favorable five-matrix with an uncontrolled adverse complement.
```

---

## Claim boundary

```text
universal infinite-to-finite theorem                 CLOSED
exact numerical calibrations                         CLOSED
actual completed-Weil cut bridge                     NEXT
native proof of memory rank <= 5                     NEXT
source-defect factorization W3-=R-VV*                NEXT
uniform no-hidden-complement theorem                 NEXT
RH                                                   NOT YET CLAIMED
```