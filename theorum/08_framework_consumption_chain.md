# Framework Consumption Chain

Use the transferred theorems in this order:

```text
01 positive eta path independence
  -> fixed five-label carrier has no positive-eta path ambiguity

02 seam integer single-sector reduction
  -> terminal obstruction is a rank-at-most-five seam packet after reduction

03 M3 positive accepted block
  -> G3/M3 accepted odd block is positive

06 one-sector theorem note
  -> even sector remains represented but is not an independent terminal gate after reduction

04 zero-cut T03 endpoint completion
  -> K0 = R_Sigma^*(I-P_src)R_Sigma >= 0

05 transfer boundary and next gate
  -> do not reopen upstream theorem packets
```

Readable framework chain:

```text
fixed five-label positive-eta path equality
+ seam-integer/five-matrix reduction
+ exact M3 accepted block
+ one-sector terminal reduction
+ zero-cut T03 source-Gram endpoint
= RKF terminal theorem surface.
```

Classical membrane, when invoked:

```text
native completed-Weil endpoint sign
+ classical completed explicit formula / Weil criterion
-> RH in declared normalization.
```

This last membrane should stay visibly marked as a terminal interface, not smuggled into the native theorem as if citation ink were proof glue.
