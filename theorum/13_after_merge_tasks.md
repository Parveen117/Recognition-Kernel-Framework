# After Merge Tasks

After this RKF branch is merged:

1. Create MP cleanup branches from the relevant MP source branches.
2. Delete only theorem/status surfaces duplicated here.
3. Keep proof-lab scripts and raw validation packets until separately archived.
4. Compare MP after cleanup to see what useful unsorted material remains.
5. Promote only the remaining useful MP material into future RKF `theorum/NN_*.md` capsules.

First cleanup target:

```text
MP branch: agent/rh-rotation-metric-topology-reset
reason:    PR #246 path-equality theorem is now cleanly transferred
```

Candidate files are listed in `MP_CLEANUP_CANDIDATES.md`.
