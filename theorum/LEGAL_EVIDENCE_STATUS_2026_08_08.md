# Legal / Proof-of-Existence Evidence Status — 2026-08-08

## Protected scope

This evidence stack commits to the theorem/proof state covering:

```text
SC-01 through SC-18
SR-01 through SR-04
```

Private proof text remains in the private IEL proof vault. The public evidence files reveal only the manifest, Git object identifiers, hashes, and independent attestations.

## Canonical subject

```text
theorum/LEGAL_EVIDENCE_MANIFEST_2026_08_08.json
```

SHA-256:

```text
7929114e18ddf99eb4a3f05b54fc38617d5d96e6f412d2b6af47ab58190fb792
```

The adjacent `.sha256` sidecar is verified in CI before third-party attestation.

## Layer 1 — Public Git commitment

The manifest was committed to the public Recognition-Kernel-Framework development branch. The manifest binds the pre-anchor public theorem head and the exact private IEL proof-vault commit/tree plus canonical proof Git blob identifiers.

This establishes a public repository history record but is not treated as the sole time authority.

## Layer 2 — GitHub / Sigstore public transparency-log attestation

Workflow:

```text
Legal Evidence Manifest Attestation
```

Workflow run:

```text
31265153379
```

Result:

```text
SUCCESS
```

The workflow independently verified the committed SHA-256 and used `actions/attest@v4` to issue a signed GitHub artifact attestation for the manifest.

Subject digest:

```text
sha256:7929114e18ddf99eb4a3f05b54fc38617d5d96e6f412d2b6af47ab58190fb792
```

The GitHub attestation API returns a Sigstore bundle with a public transparency-log entry. Its integrated time is:

```text
2026-08-08T15:43:11Z
```

This attestation is independently queryable by the subject digest through GitHub's attestation API and is not merely a repository commit timestamp.

## Layer 3 — OpenTimestamps / Bitcoin receipt

Workflow:

```text
OpenTimestamps Evidence Anchor
```

Workflow run:

```text
31265228633
```

Result:

```text
SUCCESS
```

The official `opentimestamps-client` v0.7.2 verified the manifest checksum and submitted the timestamp commitment to four remote calendars. The generated receipt is committed as:

```text
theorum/LEGAL_EVIDENCE_MANIFEST_2026_08_08.json.ots
```

Receipt SHA-256:

```text
a03f26f1aeeb92aa5fc7f8f8858129dcf9510ba62c38e3c5a48d101c1f14c8be
```

Current receipt state:

```text
PENDING BITCOIN CONFIRMATION
```

This is normal immediately after stamping. Once a calendar commitment is included in Bitcoin and sufficiently confirmed, the receipt can be upgraded and independently verified with `ots upgrade` / `ots verify`.

## Layer 4 — India PKI / CCA-regulated RFC-3161 timestamp

Recommended final regulated layer:

1. generate an RFC-3161 timestamp query over the public manifest using SHA-256;
2. submit only that request/hash to a CCA-licensed Time Stamping Service;
3. retain the returned timestamp response/token, TSA certificate chain, policy/CPS metadata, and verification output.

This layer requires subscriber/identity access to the licensed CA and therefore is not generated by repository automation.

Suggested query command:

```bash
openssl ts -query \
  -data LEGAL_EVIDENCE_MANIFEST_2026_08_08.json \
  -sha256 -cert \
  -out RNKE_2026_08_08.tsq
```

## Evidentiary boundary

These records support proof that the committed digital state existed no later than the independently attested time and that later alteration changes the digest.

They do not, by themselves, determine inventorship, authorship, ownership, patent validity, or patent priority. Where patent rights are relevant, the applicable patent filing remains the principal priority instrument.
